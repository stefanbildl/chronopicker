import { ChronoPickerDirective } from './chrono-picker.directive';

describe('ChronoPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChronoPickerDirective();
    expect(directive).toBeTruthy();
  });
});
